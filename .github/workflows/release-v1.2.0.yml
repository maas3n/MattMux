name: Release v1.2.0 (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived release
        run: |
          echo 'MattMux v1.2.0 is an immutable historical release.'
          echo 'Do not rebuild or replace its binary assets from this workflow.'
          echo 'Publish binary changes under a new version/tag instead.'
